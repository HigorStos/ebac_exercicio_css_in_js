import { Container } from '../../styles'
import { Form as FormStyled, HeroTitle } from './styles'

const Hero = () => (
  <FormStyled>
    <Container>
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </Container>
  </FormStyled>
)

export default Hero
